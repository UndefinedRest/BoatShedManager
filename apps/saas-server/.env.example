# Rowing Boards SaaS Server - Environment Variables
#
# Copy this file to .env and fill in the values.
# DO NOT commit .env to version control.
#
# IMPORTANT: Use the DEV database for local development, NEVER production.
# See DEV_SETUP.md for full instructions.

# ============================================================
# Database (Supabase PostgreSQL)
# ============================================================
# DEV:  Get from Supabase Dashboard → rowing-boards-dev → Settings → Database → Connection string (URI)
# PROD: Set in Render environment variables (never in local .env)
DATABASE_URL=postgresql://postgres.[DEV_PROJECT_REF]:[DEV_PASSWORD]@aws-0-ap-southeast-2.pooler.supabase.com:6543/postgres

# ============================================================
# Security Keys (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# ============================================================
# Use DIFFERENT keys for dev and production.
# Production keys are set in Render environment variables.
ENCRYPTION_KEY=generate-a-64-character-hex-key-for-dev
JWT_SECRET=generate-a-different-64-character-hex-key-for-dev

# ============================================================
# JWT Configuration
# ============================================================
JWT_EXPIRES_IN=3600  # Token expiry in seconds (1 hour)

# ============================================================
# Server Configuration
# ============================================================
PORT=3000
NODE_ENV=development
BASE_DOMAIN=localhost
MARKETING_URL=http://localhost:3000

# ============================================================
# Rate Limiting (requests per minute)
# ============================================================
PUBLIC_RATE_LIMIT=100
ADMIN_RATE_LIMIT=30

# ============================================================
# Swagger Documentation
# ============================================================
ENABLE_SWAGGER=true
